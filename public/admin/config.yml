backend:
  name: git-gateway
  branch: main

media_folder: "public/uploads"
public_folder: "/uploads"

collections:
  - name: "blogs"
    label: "Blogs"
    folder: "content/blogs"
    create: true
    slug: "{{slug}}"

    fields:
      - label: "Title"
        name: "title"
        widget: "string"

      - label: "Date"
        name: "date"
        widget: "datetime"

      - label: "Excerpt"
        name: "excerpt"
        widget: "text"

      - label: "Body"
        name: "body"
        widget: "markdown"

      - label: "Category"
        name: "category"
        widget: "string"

      - label: "Read Time"
        name: "readTime"
        widget: "string"
        default: "5 min read"

      - label: "Cover Image"
        name: "image"
        widget: "image"
